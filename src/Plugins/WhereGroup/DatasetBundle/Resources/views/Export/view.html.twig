{% extends 'MetadorThemeBundle::template.html.twig' %}
{% block center %}
    <style type="text/css">
        table {width: 100%; background-color: #F4F3F1;}
        .layerdata {border-bottom: 2px double black}
        td {width:50%; padding: 5px 0 5px 10px;}
        td.list{padding-left: 50px;}
        .label-head {border-bottom: 1px dashed #BBB }
        .last {border-bottom: 1px solid #BBB}
        .label {font-weight: bold; }
        .bbox-label{font-weight: bold; }
    </style>
    <div class="page-content metadataForm">
        {% include 'ProfileDatasetBundle:Export:pdf.html.twig' %}
    </div>
{% endblock %}
