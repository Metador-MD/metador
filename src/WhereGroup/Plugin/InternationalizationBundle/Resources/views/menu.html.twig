<li class="profile-select">
    <label class="profile-list-label">
        <span class="icon-flag"></span>
    </label>
    <ul class="profile-list">
        {% if locale != 'de' %}
        <li>
            <form action="{{ path('metador_locale_update', { 'locale': 'de' }) }}" method="post">
                <button class="noStyle">de</button>
            </form>
        </li>
        {% endif %}
        {% if locale != 'en' %}
        <li>
            <form action="{{ path('metador_locale_update', { 'locale': 'en' }) }}" method="post">
                <button class="noStyle">en</button>
            </form>
        </li>
        {% endif %}
    </ul>
</li>
