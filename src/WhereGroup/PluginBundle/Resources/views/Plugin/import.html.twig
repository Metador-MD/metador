{% extends 'WhereGroupThemeBundle:Template:admin.html.twig' %}

{% block title %}{% trans %}Plugin importieren{% endtrans %}{% endblock %}

{% block content %}
    {{ form_start(form, { 'action': path('metador_admin_plugin_upload'), 'method': 'POST', 'attr': {
        novalidate: 'novalidate', autocomplete: 'off'
    }}) }}

    {{ form_widget(form, {'attr': {'class': 'plugin-import'}}) }}
    {{ form_end(form) }}
{% endblock %}
