<ul>
    <li {% if controller_action() == 'index' %}class="act"{% endif %}>
        <a href="{{ path('metadata_index') }}">Übersicht</a>
    </li>
    <li {% if controller_action() == 'new' or controller_action() == 'use' %} class="act"{% endif %}>
        <a href="{{ path('metadata_new') }}">{{'neu'|trans}}</a>
    </li>

{% if controller_action() == 'edit' %}
    <li class="act">
        <span>{{'bearbeiten'|trans}}</span>
    </li>
{% endif %}

{% if controller_action() != 'index' %}
    {% if hasAccess is defined and hasAccess == 1 %}
    <li class="metadataFormSubmit"><span>speichern</span></li>
    {% endif %}
    <li><a href="{{ path('metadata_index') }}">abbrechen</a></li>
{% endif %}
</ul>
